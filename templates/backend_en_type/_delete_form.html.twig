<form method="post" action="{{ path('app_backend_en_type_delete', {'id': en_type.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ en_type.id) }}">
    <button class="btn btn-danger mt-5">Delete</button>
</form>

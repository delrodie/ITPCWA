<form method="post" action="{{ path('app_backend_en_equipe_delete', {'id': en_equipe.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ en_equipe.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>

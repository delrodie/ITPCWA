{% extends 'base.html.twig' %}

{% block title %}Cordonnee index{% endblock %}

{% block body %}
    <h1>Cordonnee index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Localisation</th>
                <th>Telephone</th>
                <th>Email</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for cordonnee in cordonnees %}
            <tr>
                <td>{{ cordonnee.id }}</td>
                <td>{{ cordonnee.localisation }}</td>
                <td>{{ cordonnee.telephone }}</td>
                <td>{{ cordonnee.email }}</td>
                <td>
                    <a href="{{ path('app_backend_cordonnee_show', {'id': cordonnee.id}) }}">show</a>
                    <a href="{{ path('app_backend_cordonnee_edit', {'id': cordonnee.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_backend_cordonnee_new') }}">Create new</a>
{% endblock %}

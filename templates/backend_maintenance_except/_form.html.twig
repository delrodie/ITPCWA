{{ form_start(form) }}
    <div class="row form-group">
        <div class="col-12 mt-3">{{ form_row(form.ip) }}</div>
        <div class="col-12 mt-3">{{ form_row(form.statut) }}</div>
    </div>
    {{ form_widget(form) }}
    <div class="text-end mt-5">
        <button class="btn btn-primary">{{ button_label|default('Enregistrer') }}</button>
    </div>

{{ form_end(form) }}

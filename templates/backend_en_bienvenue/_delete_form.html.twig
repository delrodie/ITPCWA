<form method="post" action="{{ path('app_backend_en_bienvenue_delete', {'id': en_bienvenue.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ en_bienvenue.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>

<form method="post" action="{{ path('app_backend_fr_equipe_delete', {'id': fr_equipe.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer ce memebre de l\'equipe?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ fr_equipe.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>

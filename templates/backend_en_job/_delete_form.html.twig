<form method="post" action="{{ path('app_backend_en_job_delete', {'id': en_job.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ en_job.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>

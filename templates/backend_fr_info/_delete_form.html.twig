<form method="post" action="{{ path('app_backend_fr_info_delete', {'id': fr_info.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer ce message?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ fr_info.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>

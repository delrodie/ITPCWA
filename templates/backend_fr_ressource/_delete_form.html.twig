<form method="post" action="{{ path('app_backend_fr_ressource_delete', {'id': fr_ressource.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer cette ressource?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ fr_ressource.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>

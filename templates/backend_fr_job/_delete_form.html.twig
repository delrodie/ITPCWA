<form method="post" action="{{ path('app_backend_fr_job_delete', {'id': fr_job.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer cette offre?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ fr_job.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>

<form method="post" action="{{ path('app_backend_fr_presentation_delete', {'id': fr_presentation.id}) }}" onsubmit="return confirm('Voulez-vous supprimer cette présentation?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ fr_presentation.id) }}">
    <button class="btn btn-danger mt-5">Supprimer</button>
</form>

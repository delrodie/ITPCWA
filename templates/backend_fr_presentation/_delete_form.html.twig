<form method="post" action="{{ path('app_backend_fr_presentation_delete', {'id': fr_presentation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ fr_presentation.id) }}">
    <button class="btn">Delete</button>
</form>

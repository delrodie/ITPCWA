{% extends 'base.html.twig' %}

{% block title %}EnRessource{% endblock %}

{% block body %}
    <h1>EnRessource</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ en_ressource.id }}</td>
            </tr>
            <tr>
                <th>Reference</th>
                <td>{{ en_ressource.reference }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ en_ressource.titre }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ en_ressource.description }}</td>
            </tr>
            <tr>
                <th>Media</th>
                <td>{{ en_ressource.media }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ en_ressource.slug }}</td>
            </tr>
            <tr>
                <th>Extension</th>
                <td>{{ en_ressource.extension }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ en_ressource.createdAt ? en_ressource.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ en_ressource.updatedAt ? en_ressource.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_b_ackend_en_ressource_index') }}">back to list</a>

    <a href="{{ path('app_b_ackend_en_ressource_edit', {'id': en_ressource.id}) }}">edit</a>

    {{ include('backend_en_ressource/_delete_form.html.twig') }}
{% endblock %}

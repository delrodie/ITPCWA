{{ form_start(form) }}
    <div class="row form-group">
        <div class="col-6 offset-md-3 mt-5">
            {{ form_widget(form) }}

            <button class="btn btn-primary mt-5 text-end">{{ button_label|default('Enregistrer') }}</button>
        </div>
    </div>
{{ form_end(form) }}

{% extends 'base.html.twig' %}

{% block title %}Photo{% endblock %}

{% block body %}
    <h1>Photo</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ photo.id }}</td>
            </tr>
            <tr>
                <th>Media</th>
                <td>{{ photo.media ? photo.media|join(', ') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_backend_photo_index') }}">back to list</a>

    <a href="{{ path('app_backend_photo_edit', {'id': photo.id}) }}">edit</a>

    {{ include('backend_photo/_delete_form.html.twig') }}
{% endblock %}

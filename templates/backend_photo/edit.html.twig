{% extends 'base.html.twig' %}

{% block title %}Edit Photo{% endblock %}

{% block body %}
    <h1>Edit Photo</h1>

    {{ include('backend_photo/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_backend_photo_index') }}">back to list</a>

    {{ include('backend_photo/_delete_form.html.twig') }}
{% endblock %}

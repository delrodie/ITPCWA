<form method="post" action="{{ path('app_backend_fr_projet_delete', {'id': fr_projet.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer ce projet?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ fr_projet.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>

<form method="post" action="{{ path('app_backend_fr_actualite_delete', {'id': fr_actualite.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer cette actualité?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ fr_actualite.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>

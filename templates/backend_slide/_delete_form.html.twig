<form method="post" action="{{ path('app_backend_slide_delete', {'id': slide.id}) }}" onsubmit="return confirm('Voulez-vous vraiment suprimer ce slide?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ slide.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
